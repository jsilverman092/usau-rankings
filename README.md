# USAU Rankings

## Streamlit app (local UI explorer)

Install dependencies:

```bash
pip install -r requirements.txt
```

If you prefer direct install:

```bash
pip install streamlit pandas
```

Run the app:

```bash
streamlit run app.py
```
