[project]
name = "usau-rankings"
version = "0.1.0"
description = "USA Ultimate rankings engine and simulator"
requires-python = ">=3.8"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]